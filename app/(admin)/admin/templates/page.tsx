import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function AdminTemplatesPage() {
  return (
    <RoutePlaceholder
      path="/admin/templates"
      hint="Templates list with usage stats. Built in phase 6."
    />
  );
}
