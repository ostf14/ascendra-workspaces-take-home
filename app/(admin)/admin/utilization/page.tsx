import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function AdminUtilizationPage() {
  return (
    <RoutePlaceholder
      path="/admin/utilization"
      hint="Aggregate trends + distribution chart. Built in phase 6."
    />
  );
}
