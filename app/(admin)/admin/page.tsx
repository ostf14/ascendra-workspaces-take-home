import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function AdminOverviewPage() {
  return (
    <RoutePlaceholder
      path="/admin"
      hint="Fleet overview — waste card, metrics strip, utilization chart. Built in phase 5."
    />
  );
}
