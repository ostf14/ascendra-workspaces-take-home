import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function WorkspacesPage() {
  return (
    <RoutePlaceholder
      path="/workspaces"
      hint="Developer home — list of the signed-in user's workspaces. Built in phase 4."
    />
  );
}
