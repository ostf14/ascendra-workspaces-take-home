import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function NewWorkspacePage() {
  return (
    <RoutePlaceholder
      path="/workspaces/new"
      hint="Template picker → name → confirm. Built in phase 4."
    />
  );
}
