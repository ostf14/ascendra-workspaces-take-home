import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function LoginPage() {
  return (
    <RoutePlaceholder
      path="/login"
      hint="Sign-in form lands in phase 8 polish. Phase 1 stubs the route only."
    />
  );
}
